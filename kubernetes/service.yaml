apiVersion: v1
kind: Service

metadata:
  name: ai-assistant-service
  namespace: ai-assistant-app

spec:
  selector:
    app: ai-assistant

  ports:
    - port: 80
      targetPort: 8000

  type: NodePort
